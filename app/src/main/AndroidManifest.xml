<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notekeeperkotlinofficial">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoteKeeperKotlinOfficial">
        <activity
            android:name=".ItemActivity"
            android:label="NoteKeeper"
            android:theme="@style/Theme.NoteKeeperKotlinOfficial.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NoteListActivity"
            android:label="NoteKeeper"
            android:theme="@style/Theme.NoteKeeperKotlinOfficial.NoActionBar">
        </activity>
        <activity
            android:name=".NoteActivity"
            android:label="@string/note_activity_label"
            android:theme="@style/Theme.NoteKeeperKotlinOfficial.NoActionBar">

        </activity>
    </application>

</manifest>